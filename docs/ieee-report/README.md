# IEEE Report LaTeX Package

This folder is ready to upload to Overleaf.

Files:

- `main.tex` - IEEE conference-style report.
- `references.bib` - BibTeX references used by the report.

Important SRS note:

The repository does not currently contain a final standalone SRS document. The report therefore uses a preliminary SRS baseline inferred from the repository README, API documentation, database documentation, backend documentation, and implemented UI flows.

Before final submission:

1. Replace the author email in `main.tex`.
2. Compare every `SRS-*` identifier against the final SRS.
3. Update Table `SRS Traceability Matrix` if the final SRS uses different IDs.
4. Compile on Overleaf with `main.tex` as the main file.
