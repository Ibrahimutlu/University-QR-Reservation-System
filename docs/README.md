# Documentation

Place project reports here:

- `Report1-*.pdf` — Initial design / requirements
- `Report2-*.pdf` — Conflict control mechanism
- `Report3-*.pdf` — Implementation report
- `Report4-BackendImplementation.pdf` — Backend implementation report

The Swagger UI generated by the backend at `/swagger` provides the live API contract.
